{
  "name": "upserve-demo-form",
  "version": "1.0.0",
  "private": true,
  "description": "Upserve demo request form — Webflow embed",
  "scripts": {
    "build": "node build.js",
    "test": "node build.js && playwright test",
    "test:headed": "node build.js && playwright test --headed",
    "copy": "node build.js && pbcopy < webflow/embed.html && node -e \"console.log('\\nCopied webflow/embed.html to the clipboard — paste it into the Webflow Embed element.')\"",
    "copy:revenue": "node build.js && pbcopy < webflow/embed-revenue.html && node -e \"console.log('\\nCopied webflow/embed-revenue.html to the clipboard — paste it into the Webflow Embed element.')\""
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "terser": "^5.36.0"
  }
}
